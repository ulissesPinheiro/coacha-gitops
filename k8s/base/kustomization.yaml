apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Lista todos os recursos da base
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
